---
description: Logging and analytics.
globs:
    - "Core/Observability/**"
---
- Use os.Logger with subsystem "com.amitsen.swiftboard".
- Create a tiny Analytics protocol. Default is no-op.
- Log key flows: auth success/failure, refresh, dashboard load, task toggle.
- Redact PII. Verify with a unit test that redaction occurs.
- Analytics must not crash on failure.