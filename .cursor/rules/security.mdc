---
description: Keychain and PII rules.
globs:
    - "Platform/Security/**"
    - "Core/Security/**"
---
- Store accessToken and refreshToken in Keychain.
- Add a thin Keychain wrapper with unit tests.
- Never log tokens or emails. Redact with "***".
- Use Secure Enclave if not needed. Keep it simple for demo.
- When signing out, wipe tokens and in-memory user data.